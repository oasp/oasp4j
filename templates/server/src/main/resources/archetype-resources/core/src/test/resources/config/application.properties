${symbol_pound} https://docs.spring.io/spring-boot/docs/current/reference/html/howto-embedded-servlet-containers.html${symbol_pound}howto-user-a-random-unassigned-http-port
server.port=0
${symbol_pound}Comment the following line in the case of database other than in-memory database H2
spring.profiles.active=junit

${symbol_pound}UnComment the following line in the case of MS SQL Server 2008 database
${symbol_pound}spring.profiles.active=junit,mssql

${symbol_pound}UnComment the following line in the case of Oracle 11g database
${symbol_pound}spring.profiles.active=junit,orcl

${symbol_pound}UnComment the following line in the case of PostGres 9.5.4 database
${symbol_pound}spring.profiles.active=junit,postgres

${symbol_pound}UnComment the following profile 'mysql' if the database used is MariaDB 10.0.27
${symbol_pound}spring.profiles.active=junit,mysql

${symbol_pound} Flyway for Database Setup and Migrations
flyway.enabled=true
flyway.clean-on-validation-error=true
flyway.validate-on-migrate=false
${symbol_pound}flyway.baseline-on-migrate=true

${symbol_pound}the following line configures both src/test/resources and src/main/resources as locations for migration scripts
${symbol_pound}flyway.locations=db/migration,AllTests/setup/db

${symbol_pound} Flyway for setting up testdata in database
database.migration.auto=false
database.migration.testdata=false
database.migration.clean=false


${symbol_pound}UnComment the following profile 'mysql' if the database used is MariaDB 10.0.27
${symbol_pound}spring.profiles.active=junit,mysql

${symbol_pound} Service configuration
service.client.app.restaurant.auth=basic
service.client.app.restaurant.host=localhost