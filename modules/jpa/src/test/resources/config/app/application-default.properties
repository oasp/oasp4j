# ---------------------------------------------------------------------------
# Persistence and database
# ---------------------------------------------------------------------------
database.user.login = root
database.user.password =root
database.url = jdbc:mysql://localhost:3306/restaurant?user=root&password=root&connectTimeout=10000&socketTimeout=10000&autoReconnect=true
database.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
database.datasource = javax.sql.DataSource
database.hibernate.hbm2ddl.auto = create
database.hibernate.show.sql = true

# Activate spring profiles
#spring.profiles.active = integrationTest
